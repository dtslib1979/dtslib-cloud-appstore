# DTS Cloud AppStore - System Architecture Documentation

## 📋 목차 (Table of Contents)

1. [시스템 개요 (System Overview)](#시스템-개요-system-overview)
2. [설치 및 구성 이력 (Installation & Configuration History)](#설치-및-구성-이력-installation--configuration-history)
3. [시스템 아키텍처 (System Architecture)](#시스템-아키텍처-system-architecture)
4. [기술 스택 (Technology Stack)](#기술-스택-technology-stack)
5. [애플리케이션 구성 (Application Components)](#애플리케이션-구성-application-components)
6. [배포 파이프라인 (Deployment Pipeline)](#배포-파이프라인-deployment-pipeline)
7. [현재 이슈 및 문제점 (Current Issues)](#현재-이슈-및-문제점-current-issues)
8. [디렉토리 구조 (Directory Structure)](#디렉토리-구조-directory-structure)

---

## 시스템 개요 (System Overview)

### 프로젝트명
**DTS Cloud AppStore** - 박씨 전용 PWA 공장 (모바일 도구 앱스토어)

### 목적
GitHub Pages를 통해 배포되는 Progressive Web App(PWA) 기반의 모바일 비디오 편집 도구 모음집

### 접속 URL
- **메인 AppStore**: https://dtslib1979.github.io/dtslib-cloud-appstore/
- **Shorts Maker**: https://dtslib1979.github.io/dtslib-cloud-appstore/shorts-maker/
- **Auto Shorts Maker v1.1**: https://dtslib1979.github.io/dtslib-cloud-appstore/auto-shorts-maker/

### 주요 특징
- 완전한 클라이언트 사이드 처리 (서버 불필요)
- PWA 기술로 오프라인 사용 가능
- 모바일 최적화 UI/UX
- FFmpeg.wasm을 활용한 브라우저 기반 비디오 처리

---

## 설치 및 구성 이력 (Installation & Configuration History)

### 타임라인

#### 2025-12-03
**Initial Repository Setup & Deployment Configuration**

1. **저장소 생성 및 초기 구성**
   - GitHub 저장소 생성: `dtslib1979/dtslib-cloud-appstore`
   - 기본 프로젝트 구조 설정
   - README.md 작성

2. **GitHub Actions 배포 워크플로우 추가**
   - 파일: `.github/workflows/deploy.yml`
   - 목적: main 브랜치로의 push 시 자동으로 GitHub Pages에 배포
   - 액션: `peaceiris/actions-gh-pages@v3` 사용
   - 배포 브랜치: `gh-pages`

3. **애플리케이션 개발 및 추가**
   - **Shorts Maker (기존 버전)** 구현
     - 6초 영상을 2분 쇼츠로 변환
     - 0.2x 슬로우모션 적용
     - 자동 반복 및 오디오 합성
   
   - **Auto Shorts Maker v1.1 (신규)** 구현
     - 6~10초 영상을 2분 쇼츠로 자동 변환
     - SharedArrayBuffer 불필요 (싱글스레드 ffmpeg.wasm)
     - 동적 반복 계산 로직
     - 향상된 에러 처리

4. **PWA 기능 구현**
   - Service Worker 설정 (오프라인 캐싱)
   - Web App Manifest 구성
   - 모바일 설치 지원

5. **GitHub Pages 설정**
   - `.nojekyll` 파일 추가 (Jekyll 빌드 비활성화)
   - 정적 사이트 호스팅 활성화

### 설치된 외부 의존성

#### CDN을 통한 라이브러리
1. **FFmpeg.wasm v0.11.6**
   - 용도: 브라우저 기반 비디오 인코딩/디코딩
   - shorts-maker: `https://cdn.jsdelivr.net/npm/@ffmpeg/ffmpeg@0.11.6/dist/ffmpeg.min.js`
   - auto-shorts-maker: `https://unpkg.com/@ffmpeg/ffmpeg@0.11.6/dist/ffmpeg.min.js`

2. **FFmpeg Core v0.11.0**
   - 용도: FFmpeg.wasm의 WebAssembly 코어
   - auto-shorts-maker: `https://unpkg.com/@ffmpeg/core@0.11.0/dist/ffmpeg-core.js`

### 구성 파일

#### GitHub Actions 워크플로우
- **파일**: `.github/workflows/deploy.yml`
- **트리거**: main 브랜치 push, 수동 실행 (workflow_dispatch)
- **권한**: contents: write
- **주요 설정**:
  - 배포 디렉토리: 루트 (`.`)
  - 배포 브랜치: `gh-pages`
  - 제외 디렉토리: `.github`

#### PWA Manifest 파일
1. **shorts-maker/manifest.json**
   - 앱 이름: "Auto Shorts Maker"
   - 테마 색상: #764ba2
   - Start URL: `/dtslib-cloud-appstore/shorts-maker/`

2. **auto-shorts-maker/manifest.json**
   - 앱 이름: "Auto Shorts Maker v1.1"
   - 테마 색상: #667eea
   - Start URL: `./`
   - 화면 방향: portrait

---

## 시스템 아키텍처 (System Architecture)

### 전체 시스템 구조

```
┌─────────────────────────────────────────────────────────┐
│                    GitHub Repository                     │
│                 dtslib1979/dtslib-cloud-appstore         │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ git push to main
                  ▼
┌─────────────────────────────────────────────────────────┐
│              GitHub Actions Workflow                     │
│                  (.github/workflows/deploy.yml)          │
│                                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │  1. Checkout Repository                         │   │
│  │  2. Deploy to gh-pages branch                   │   │
│  │     (using peaceiris/actions-gh-pages@v3)       │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ deploy
                  ▼
┌─────────────────────────────────────────────────────────┐
│                   GitHub Pages                           │
│         https://dtslib1979.github.io/                    │
│              dtslib-cloud-appstore/                      │
│                                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │           AppStore Dashboard                     │   │
│  │             (index.html)                         │   │
│  │                                                   │   │
│  │  ┌─────────────┐      ┌──────────────────┐     │   │
│  │  │   Shorts    │      │  Auto Shorts     │     │   │
│  │  │   Maker     │      │  Maker v1.1      │     │   │
│  │  │             │      │                  │     │   │
│  │  └─────────────┘      └──────────────────┘     │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ user access
                  ▼
┌─────────────────────────────────────────────────────────┐
│                  User's Browser                          │
│                                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Service Worker (캐싱 및 오프라인 지원)         │   │
│  ├─────────────────────────────────────────────────┤   │
│  │  PWA Application (HTML/CSS/JS)                   │   │
│  ├─────────────────────────────────────────────────┤   │
│  │  FFmpeg.wasm (WebAssembly)                       │   │
│  │  - 비디오 디코딩/인코딩                          │   │
│  │  - 오디오 처리                                   │   │
│  │  - 파일 변환                                     │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 애플리케이션 아키텍처 (각 PWA)

```
┌────────────────────────────────────────────────────────┐
│                   PWA Application                       │
│                                                          │
│  ┌──────────────────────────────────────────────┐     │
│  │  Presentation Layer (UI)                     │     │
│  │  - HTML5 (구조)                              │     │
│  │  - CSS3 (스타일링, 모바일 최적화)            │     │
│  │  - Responsive Design                         │     │
│  └──────────────────┬───────────────────────────┘     │
│                     │                                   │
│  ┌──────────────────▼───────────────────────────┐     │
│  │  Application Logic Layer                     │     │
│  │  - JavaScript (ES6+)                         │     │
│  │  - 파일 업로드 처리                          │     │
│  │  - 진행률 관리                               │     │
│  │  - 에러 핸들링                               │     │
│  └──────────────────┬───────────────────────────┘     │
│                     │                                   │
│  ┌──────────────────▼───────────────────────────┐     │
│  │  Video Processing Layer                      │     │
│  │  - FFmpeg.wasm Integration                   │     │
│  │  - 비디오 디코딩/인코딩                      │     │
│  │  - 오디오 합성                               │     │
│  │  - 반복 로직 처리                            │     │
│  └──────────────────┬───────────────────────────┘     │
│                     │                                   │
│  ┌──────────────────▼───────────────────────────┐     │
│  │  PWA Infrastructure Layer                    │     │
│  │  - Service Worker (캐싱)                     │     │
│  │  - Web App Manifest                          │     │
│  │  - 오프라인 지원                             │     │
│  └──────────────────────────────────────────────┘     │
└────────────────────────────────────────────────────────┘
```

### 데이터 흐름 (Video Processing Flow)

```
[사용자] 
   │
   ▼
[1. 파일 선택 (Video + Audio)]
   │
   ▼
[2. 파일 검증]
   │
   ▼
[3. FFmpeg.wasm 로드]
   │
   ▼
[4. 파일을 FFmpeg 가상 파일시스템에 작성]
   │
   ▼
[5. 비디오 처리]
   │
   ├─ Shorts Maker: 
   │  └─ 0.2x 슬로우모션 → 반복 → 오디오 합성
   │
   └─ Auto Shorts Maker v1.1:
      └─ 음소거 → 동적 반복 계산 → 반복 → 오디오 합성
   │
   ▼
[6. FFmpeg 인코딩 (libx264, AAC)]
   │
   ▼
[7. 결과 파일 읽기 (가상 FS)]
   │
   ▼
[8. Blob 생성 및 다운로드 링크 제공]
   │
   ▼
[사용자 다운로드]
```

---

## 기술 스택 (Technology Stack)

### Frontend
- **HTML5**: 구조 및 마크업
- **CSS3**: 스타일링 및 반응형 디자인
  - Flexbox/Grid 레이아웃
  - 그라디언트 효과
  - 모바일 우선 디자인
- **JavaScript (ES6+)**: 애플리케이션 로직
  - Async/Await
  - File API
  - Blob API

### 비디오 처리
- **FFmpeg.wasm v0.11.6**: WebAssembly 기반 미디어 처리
  - 싱글스레드 버전 (SharedArrayBuffer 불필요)
  - libx264 비디오 코덱
  - AAC 오디오 코덱

### PWA 기술
- **Service Worker**: 오프라인 캐싱 및 백그라운드 동기화
- **Web App Manifest**: 설치 가능한 앱 메타데이터
- **Cache API**: 정적 자산 캐싱

### CI/CD
- **GitHub Actions**: 자동 배포 파이프라인
- **GitHub Pages**: 정적 사이트 호스팅

### 외부 의존성
| 라이브러리 | 버전 | 용도 | CDN | 상태 |
|-----------|------|------|-----|------|
| FFmpeg.wasm | 0.11.6 | 비디오 처리 | unpkg.com / cdn.jsdelivr.net | ⚠️ 불일치 (통일 필요) |
| FFmpeg Core | 0.11.0 | WASM 런타임 | unpkg.com | ✅ 정상 |

> **⚠️ 주의**: 현재 두 앱에서 서로 다른 CDN을 사용 중입니다. [ISSUES.md](./ISSUES.md#2-ffmpegwasm-cdn-불일치)를 참조하여 통일이 필요합니다.

---

## 애플리케이션 구성 (Application Components)

### 1. AppStore Dashboard (메인 화면)

**위치**: `index.html`

**기능**:
- 앱 카탈로그 표시
- 각 PWA로의 네비게이션

**구성요소**:
- 그라디언트 카드 UI
- 반응형 그리드 레이아웃
- 앱 아이콘 및 설명

---

### 2. Shorts Maker (기존 버전)

**위치**: `shorts-maker/`

**주요 파일**:
```
shorts-maker/
├── index.html          # UI 구조
├── script.js           # 비디오 처리 로직 (127 lines)
├── style.css           # 스타일 (154 lines)
├── manifest.json       # PWA 메타데이터
└── service-worker.js   # 캐싱 로직 (43 lines)
```

**기능**:
- 6~10초 짧은 영상 업로드
- 2분 음원 업로드
- 자동 슬로우모션 적용 (0.2x)
- 120초 맞춤 자동 반복
- 오디오 자동 합성
- MP4 다운로드

**처리 과정**:
```javascript
// 1. 영상을 0.2x 슬로우모션으로 변환 (6초 → 30초)
// 2. 30초 영상을 4회 반복 (30초 x 4 = 120초)
// 3. 2분 오디오 합성
// 4. MP4로 인코딩
```

**FFmpeg 명령어 예시**:
```bash
# 슬로우모션 + 반복
ffmpeg -i input.mp4 -filter_complex "[0:v]setpts=5*PTS[slow];[slow]loop=4:1:0" output.mp4

# 오디오 합성
ffmpeg -i video.mp4 -i audio.mp3 -c:v copy -c:a aac output.mp4
```

---

### 3. Auto Shorts Maker v1.1 (신규 버전)

**위치**: `auto-shorts-maker/`

**주요 파일**:
```
auto-shorts-maker/
├── index.html          # UI 구조
├── app.js              # 비디오 처리 로직 (223 lines)
├── style.css           # 스타일 (212 lines)
├── manifest.json       # PWA 메타데이터
└── service-worker.js   # 캐싱 로직 (81 lines)
```

**기능**:
- 6~10초 짧은 영상 업로드 (가변 길이)
- 2분 음원 업로드
- **동적 반복 계산**: `loops = ceil(120 / video_duration)`
- 원본 영상 자동 음소거
- 120초 정확 반복
- 오디오 자동 덮어씌우기
- MP4 다운로드

**개선 사항**:
1. **SharedArrayBuffer 불필요**
   - 싱글스레드 FFmpeg.wasm 사용
   - CORS 헤더 설정 불필요
   - 모바일 브라우저 호환성 향상

2. **동적 반복 로직**
   ```javascript
   // 예시:
   // 6초 영상 → 20회 반복 (ceil(120/6) = 20)
   // 7초 영상 → 18회 반복 (ceil(120/7) = 18)
   // 10초 영상 → 12회 반복 (ceil(120/10) = 12)
   ```

3. **향상된 에러 처리**
   - 단계별 진행률 표시
   - 상세한 에러 메시지
   - 에러 복구 메커니즘

**처리 과정**:
```javascript
// 1. 비디오 메타데이터 읽기 (duration)
// 2. 반복 횟수 계산: loops = ceil(120 / duration)
// 3. 비디오 음소거 및 반복
//    ffmpeg -i input.mp4 -an -filter_complex "loop=loops:1:0" muted.mp4
// 4. 오디오 합성
//    ffmpeg -i muted.mp4 -i audio.mp3 -c:v copy -c:a aac output.mp4
```

**FFmpeg 인코딩 옵션**:
```bash
Video:
- Codec: libx264
- CRF: 18 (고품질)
- Preset: medium
- Pixel Format: yuv420p (호환성)

Audio:
- Codec: AAC
- Bitrate: 192kbps
```

---

## 배포 파이프라인 (Deployment Pipeline)

### GitHub Actions Workflow

**파일**: `.github/workflows/deploy.yml`

**트리거**:
- main 브랜치로 push
- 수동 실행 (workflow_dispatch)

**단계**:
```yaml
1. Checkout Repository
   - actions/checkout@v4
   - 소스 코드 체크아웃

2. Deploy to GitHub Pages
   - peaceiris/actions-gh-pages@v3
   - gh-pages 브랜치로 배포
   - 배포 디렉토리: 프로젝트 루트
   - .github 디렉토리 제외
```

**배포 흐름**:
```
[Developer] 
    │
    ▼
[git commit & push to main]
    │
    ▼
[GitHub Actions Triggered]
    │
    ├─ Step 1: Checkout code
    │
    └─ Step 2: Deploy to gh-pages
         │
         ▼
[GitHub Pages Server]
    │
    ▼
[User Access via HTTPS]
```

### 배포 설정

**GitHub Pages 설정**:
- Source: gh-pages 브랜치
- 루트 디렉토리: `/`
- Custom Domain: 없음
- HTTPS 강제: 활성화

**.nojekyll 파일**:
- Jekyll 빌드 프로세스 비활성화
- 모든 파일을 그대로 배포

---

## 현재 이슈 및 문제점 (Current Issues)

### 🔴 중요 이슈

#### 1. Service Worker 스코프 불일치
**문제**:
- `shorts-maker/manifest.json`의 `start_url`이 절대 경로 사용: `/dtslib-cloud-appstore/shorts-maker/`
- `auto-shorts-maker/manifest.json`은 상대 경로 사용: `./`
- 일관성 없는 경로 설정으로 PWA 설치 문제 가능성

**영향**:
- PWA 설치 시 경로 오류
- Service Worker 등록 실패 가능성
- 오프라인 모드 작동 불안정

**해결 방안**:
```json
// shorts-maker/manifest.json 수정 필요
{
  "start_url": "./",  // 상대 경로로 변경
  "scope": "./"       // 상대 경로로 변경
}
```

#### 2. FFmpeg.wasm CDN 불일치
**문제**:
- shorts-maker: `cdn.jsdelivr.net` 사용
- auto-shorts-maker: `unpkg.com` 사용
- 다른 CDN 사용으로 일관성 부족

**영향**:
- CDN 장애 시 서로 다른 영향
- 버전 관리 복잡도 증가
- 캐싱 효율성 저하

**해결 방안**:
- 모든 앱에서 동일한 CDN 사용 (unpkg.com 권장)
- 또는 로컬 호스팅 고려

#### 3. Service Worker 캐싱 전략 차이
**문제**:
- shorts-maker: 기본적인 cache-first 전략
- auto-shorts-maker: 고급 캐싱 (CDN 제외, 에러 처리)
- 일관성 없는 캐싱 전략

**영향**:
- 사용자 경험 차이
- 유지보수 복잡도

**해결 방안**:
- auto-shorts-maker의 개선된 Service Worker를 shorts-maker에도 적용

---

### 🟡 개선 권장사항

#### 4. 에러 처리 부족 (shorts-maker)
**문제**:
- auto-shorts-maker에는 에러 섹션 있음
- shorts-maker에는 에러 UI 부재

**해결 방안**:
```html
<!-- shorts-maker/index.html에 추가 -->
<div class="error-section" id="error-section" style="display: none;">
    <p class="error-text" id="error-text"></p>
</div>
```

#### 5. 진행률 표시 개선
**문제**:
- 현재 진행률은 단순 텍스트
- FFmpeg 실제 진행률과 동기화 안 됨

**해결 방안**:
- FFmpeg.wasm의 progress 이벤트 활용
- 실시간 진행률 업데이트

#### 6. 파일 크기 제한 없음
**문제**:
- 대용량 파일 업로드 시 메모리 부족 가능
- 브라우저 크래시 위험

**해결 방안**:
```javascript
// 파일 크기 검증 추가
const MAX_FILE_SIZE = 500 * 1024 * 1024; // 500MB
if (videoFile.size > MAX_FILE_SIZE) {
    showError('File too large. Max 500MB');
    return;
}
```

#### 7. 브라우저 호환성 검사 없음
**문제**:
- WASM 지원 여부 확인 안 함
- 구형 브라우저에서 오류 발생 가능

**해결 방안**:
```javascript
if (!window.WebAssembly) {
    alert('Your browser does not support WebAssembly');
    return;
}
```

#### 8. 모바일 메모리 최적화 부족
**문제**:
- 모바일 기기의 제한된 메모리 고려 부족
- 처리 후 메모리 정리 불명확

**해결 방안**:
```javascript
// 처리 완료 후 명시적 메모리 해제
ffmpeg.FS('unlink', 'input.mp4');
ffmpeg.FS('unlink', 'audio.mp3');
ffmpeg.FS('unlink', 'output.mp4');
```

---

### 🟢 향후 개선 계획

#### 9. 테스트 부재
**문제**: 자동화된 테스트 없음

**제안**:
- Jest/Vitest를 이용한 단위 테스트
- Playwright를 이용한 E2E 테스트
- GitHub Actions에 테스트 통합

#### 10. 모니터링 부재
**문제**: 사용자 오류 추적 불가

**제안**:
- Sentry 또는 Google Analytics 통합
- 에러 로깅 및 분석

#### 11. 성능 최적화
**제안**:
- Lazy Loading (FFmpeg.wasm)
- Code Splitting
- 이미지 최적화
- Lighthouse 점수 개선

---

## 디렉토리 구조 (Directory Structure)

```
dtslib-cloud-appstore/
│
├── .github/
│   └── workflows/
│       └── deploy.yml              # GitHub Actions 배포 워크플로우
│
├── shorts-maker/                   # Shorts Maker PWA (기존 버전)
│   ├── index.html                  # UI 구조 (52 lines)
│   ├── script.js                   # 비디오 처리 로직 (127 lines)
│   ├── style.css                   # 스타일시트 (154 lines)
│   ├── manifest.json               # PWA 매니페스트
│   └── service-worker.js           # 캐싱 및 오프라인 지원 (43 lines)
│
├── auto-shorts-maker/              # Auto Shorts Maker v1.1 (신규 버전)
│   ├── index.html                  # UI 구조 (64 lines)
│   ├── app.js                      # 비디오 처리 로직 (223 lines)
│   ├── style.css                   # 스타일시트 (212 lines)
│   ├── manifest.json               # PWA 매니페스트
│   └── service-worker.js           # 향상된 캐싱 로직 (81 lines)
│
├── index.html                      # AppStore 대시보드 (85 lines)
├── README.md                       # 프로젝트 소개 및 사용법
├── ARCHITECTURE.md                 # 시스템 아키텍처 문서 (이 파일)
└── .nojekyll                       # Jekyll 비활성화

총 파일 수: 14개
총 코드 라인: 840+ lines (JS/CSS only)
```

---

## 보안 고려사항 (Security Considerations)

### 현재 보안 상태

✅ **양호한 부분**:
- 완전한 클라이언트 사이드 처리 (서버 측 취약점 없음)
- HTTPS 강제 (GitHub Pages)
- 외부 입력 검증 (파일 타입)
- XSS 공격 벡터 최소화

⚠️ **주의 필요**:
- 사용자 파일이 브라우저 메모리에서만 처리됨 (개인정보 보호 우수)
- 하지만 악의적 파일 업로드 가능성
- FFmpeg.wasm 취약점 의존성

### 권장사항
1. Content Security Policy (CSP) 헤더 추가
2. Subresource Integrity (SRI) 해시 사용
3. 정기적인 의존성 업데이트

---

## 성능 메트릭 (Performance Metrics)

### 예상 성능 (실측 필요)

| 지표 | 예상 값 | 비고 |
|------|---------|------|
| First Contentful Paint | < 1.5s | 정적 HTML/CSS |
| Time to Interactive | < 3s | FFmpeg 로드 전 |
| FFmpeg 로드 시간 | 3-5s | 네트워크 의존 |
| 6초 영상 처리 시간 | 30-60s | 디바이스 성능 의존 |
| 메모리 사용량 | ~200MB | 영상 크기 의존 |

### 최적화 기회
- Service Worker 프리캐싱
- FFmpeg.wasm lazy loading
- 번들 최소화 (현재 미적용)

---

## 유지보수 가이드 (Maintenance Guide)

### 의존성 업데이트
```bash
# FFmpeg.wasm 버전 확인
# https://github.com/ffmpegwasm/ffmpeg.wasm/releases

# 업데이트 시 변경 필요 파일:
# - auto-shorts-maker/index.html (line 61)
# - auto-shorts-maker/app.js (line 5)
# - shorts-maker/index.html (line 49)
```

### 새 앱 추가 시
1. 새 디렉토리 생성 (예: `new-app/`)
2. 필수 파일 생성:
   - index.html
   - manifest.json
   - service-worker.js
   - style.css
   - app.js
3. 메인 `index.html`에 앱 카드 추가
4. README.md 업데이트
5. main 브랜치에 push (자동 배포)

### 버그 수정 프로세스
1. 이슈 생성 및 재현
2. 로컬에서 수정
3. 브랜치 생성 및 커밋
4. Pull Request 생성
5. 리뷰 후 main에 머지
6. 자동 배포 확인

---

## 라이선스 및 크레딧 (License & Credits)

### 제작자
**Parksy CTO (Uncle, Parksy)**
- Email: dtslib1979@gmail.com
- GitHub: [@dtslib1979](https://github.com/dtslib1979)

### 사용된 오픈소스
- [FFmpeg.wasm](https://github.com/ffmpegwasm/ffmpeg.wasm) - LGPL v2.1
- [GitHub Pages](https://pages.github.com/) - GitHub ToS
- [peaceiris/actions-gh-pages](https://github.com/peaceiris/actions-gh-pages) - MIT

---

## 연락처 (Contact)

문제 발생 시:
1. GitHub Issues에 보고
2. 이메일: dtslib1979@gmail.com

---

**문서 버전**: 1.0  
**최종 업데이트**: 2025-12-03  
**작성자**: Copilot Agent  
